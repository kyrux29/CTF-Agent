[project]
name = "ctfmesh-knowledge"
version = "0.1.0"
description = "Digest-pinned local CTF knowledge retrieval"
license = "MIT"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ctfmesh_knowledge"]
