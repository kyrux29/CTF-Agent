FROM python:3.12.4-slim-bookworm@sha256:a3e58f9399353be051735f09be0316bfdeab571a5c6a24fd78b92df85bcb2d85

# Test-only dependencies. This image never receives an API key, challenge, or
# Docker socket; it can reach only the isolated P3 echo service at runtime.
RUN pip install --no-cache-dir \
    docker==7.1.0 \
    fastapi==0.115.12 \
    httpx==0.28.1 \
    pydantic==2.11.4 \
    pydantic-settings==2.9.1 \
    pytest==8.3.5 \
    pytest-asyncio==0.24.0

WORKDIR /workspace
