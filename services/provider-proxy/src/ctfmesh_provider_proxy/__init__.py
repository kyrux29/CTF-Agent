"""Restricted provider egress service for the CTFMesh M3 deployment.

Imports intentionally stay empty: ``python -m ctfmesh_provider_proxy.server``
must not pre-import its executable module while Python is resolving it.
"""

__all__: tuple[str, ...] = ()
