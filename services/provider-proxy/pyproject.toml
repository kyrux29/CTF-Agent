[project]
name = "ctfmesh-provider-proxy"
version = "0.1.0"
description = "Exact-host CONNECT egress proxy for the isolated CTFMesh Pi runner"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ctfmesh_provider_proxy"]
