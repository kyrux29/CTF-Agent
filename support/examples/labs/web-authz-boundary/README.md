# web-authz-boundary

Local M5 evaluation target for object-level authorization checks with two local
identities. It is optional and receives a per-reset flag only through its own
read-only named volume.
